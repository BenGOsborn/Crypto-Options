{
    "name": "src",
    "version": "1.0.0",
    "scripts": {
        "fork": "ganache-cli --fork https://mainnet.infura.io/v3/${INFURA} --unlock ${MAINNET_ACCOUNT} --networkId 999",
        "test": "npx truffle test --network mainnet_fork"
    },
    "devDependencies": {
        "@openzeppelin/contracts": "^4.3.2",
        "truffle": "^5.4.11"
    }
}
