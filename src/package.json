{
    "name": "src",
    "version": "1.0.0",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@openzeppelin/contracts": "^4.3.2",
        "truffle": "^5.4.11"
    }
}
