{
    "name": "src",
    "version": "1.0.0",
    "scripts": {
        "fork": "npx ganache-cli --fork https://mainnet.infura.io/v3/$INFURA --unlock $STABLECOIN_WHALE --networkId 999",
        "test": "npx truffle test --network mainnet_fork"
    },
    "devDependencies": {
        "@openzeppelin/contracts": "^4.3.2",
        "dotenv": "^10.0.0",
        "ganache-cli": "^6.12.2",
        "truffle": "^5.4.11"
    }
}
