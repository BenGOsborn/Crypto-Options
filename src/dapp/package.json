{
    "name": "src",
    "version": "1.0.0",
    "scripts": {
        "compile": "npx truffle compile",
        "fork": "npx ganache-cli --fork https://mainnet.infura.io/v3/$INFURA --unlock $STABLECOIN_WHALE --unlock $TOKEN_WHALE --networkId 999",
        "test": "npx truffle test --network mainnet_fork",
        "test:event": "npx truffle test --network mainnet_fork --show-events",
        "deploy:ropsten": "npx truffle migrate --network ropsten"
    },
    "devDependencies": {
        "@openzeppelin/contracts": "^4.3.2",
        "@openzeppelin/test-helpers": "^0.5.13",
        "@truffle/hdwallet-provider": "^1.5.0",
        "dotenv": "^10.0.0",
        "ganache-cli": "^6.12.2",
        "truffle": "^5.4.11"
    }
}
